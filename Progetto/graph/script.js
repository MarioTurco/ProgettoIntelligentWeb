function renderLabelOnTheSide(filepath){
    var par = Document.getElementById(labelToShow);
    console.log(filepath)
   // var text = "<embed src="+filepath+"></embed>";
   // par.appendChild(text);
}